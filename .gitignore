# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Firebase service account key — admin credential, never commit or bundle
trading.json
*-firebase-adminsdk-*.json
serviceAccount*.json

# Local environment (Firebase web config lives here)
.env
.env.local
.env.*.local
email-preview.html
# Backend secrets — the service account is a full-access credential.
backend/.env
backend/.env.*
!backend/.env.example
backend/.venv/
backend/__pycache__/

# Destructive one-off: deletes every account in the project.
scripts/wipe-accounts.mjs
