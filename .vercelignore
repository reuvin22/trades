# Admin credentials must never reach a build environment or a deployment.
trading.json
*-firebase-adminsdk-*.json
serviceAccount*.json

# Env values come from Vercel project settings, not from uploaded files.
.env
.env.local
.env.*.local

node_modules
dist

# The FastAPI service deploys separately; it has no place in the web bundle.
backend
